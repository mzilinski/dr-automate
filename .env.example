# dr-automate Umgebungsvariablen
# Kopiere diese Datei zu .env und passe die Werte an

# Server-Konfiguration
PORT=5001
HOST=0.0.0.0

# Debug-Modus (true/false) - NICHT in Produktion aktivieren!
FLASK_DEBUG=false

# Pfad zur PDF-Vorlage (relativ zum Projektverzeichnis)
PDF_TEMPLATE_PATH=forms/DR-Antrag_035_001Stand4-2025pdf.pdf

# Secret Key für CSRF-Schutz und Sessions
# Generiere einen sicheren Key mit: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-change-in-production

# Rate Limiting (Requests pro Minute für /generate Endpunkt)
RATE_LIMIT=10
